[package]
name = "tree-sitter-loom"
version = "0.1.0"
edition = "2021"

build = "bindings/rust/build.rs"

[lib]
path = "bindings/rust/lib.rs"

[dependencies]
tree-sitter-language = "0.1"

[build-dependencies]
cc = "1.2"
