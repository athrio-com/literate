$schema: 'https://moonrepo.dev/schemas/project.json'

language: typescript
type: application

tasks:
  dev:
    command: 'next dev'
    platform: system
    local: true

  build:
    command: 'next build'
    outputs:
      - '.next'

  typecheck:
    command: 'tsc --noEmit'
