# Kinds test fixture

Placeholder prose sibling used by `kinds.test.ts` to construct a
Concept, a Subkind, a Member, and a Trope. The tests assert
constructor wiring; they do not exercise `.load()`, so the body
of this file is not read.
