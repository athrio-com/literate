{
  "name": "@literate/template-minimal",
  "version": "0.0.1",
  "description": "Minimal Literate Framework consumer scaffold — corpus/ + .literate/ + manifest. Shipped as read-only resources copied by the CLI init verb (deferred) or directly by tooling.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src/**",
    "files/**"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/bun": "latest"
  }
}
