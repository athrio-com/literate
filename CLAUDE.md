# Literate Framework (LF) — Maintainer Entry Point

*This file orients AI collaborators working on LF itself. It is
deliberately short: its job is to send you to the right Protocol file
for the work you're about to do.*

---

## Before any action — Mandatory Agent Instructions

These steps are imperative, in order, and override any urge to begin
investigating the repo from the prompt alone. The full agent
procedure lives in [`corpus/LITERATE.md`](./corpus/LITERATE.md).

The repository is at the LP-revision baseline: prior scaffolding has
been archived under `temp/legacy/` (gitignored). The next milestone is
the seed `tangle` CLI plus the first Concept Manifests under
`corpus/`. Until those exist, ask the Person before authoring anything.
