# Specs — index

Behavioural specifications for LF-the-project (CLI verbs, compilation,
site routes). Mutable; each new file and material revision passes the
review gate.

| Slug | Purpose | Status |
|---|---|---|

*Empty at v0.1. First spec will land when the CLI verbs are refined
into formal behaviour contracts.*
