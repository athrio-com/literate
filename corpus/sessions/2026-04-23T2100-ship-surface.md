# Session: 2026-04-23 — LF's ship surface (install, publish, consumer invariant)

**Date:** 2026-04-23
**Started:** 2026-04-23T21:00
**Status:** Closed (2026-04-24T17:00)
**Chapter:** — (no chapter yet)
**Agent:** Claude Opus 4.7 (1M context)

## Pre-work

Per `session-start` (spontaneous path, IMP-1):

- **Last `Status: Closed` session.** S4 closed at
  `2026-04-23T1920-cli-template-and-e2e-smoke` (Closed
  2026-04-23T20:30). Summary: MVP capstone shipped — `@literate/cli`
  dispatches workflow Tropes over a live `node:fs`-backed
  `SessionStore`, `@literate/template-minimal` provides the consumer
  scaffold, and an end-to-end smoke test exercises scaffold → open →
  gate → populate → close. 30/30 tests pass across 5 packages. The
  MVP arc (S1 → S2 → S3 → S4) is closed. LF is demonstrably runnable
  end-to-end by a consumer *who checks out this repo* — but nothing
  is published anywhere and consumers have no install path.

- **Relevant deferred / discovered from S4.**
  - `literate init` verb deferred past MVP (consumers must scaffold
    manually or call `scaffold()` from JS). This session likely
    touches it once the template's shape is finalised.
  - No release engineering exists: `packages/*` use
    `workspace:*` specifiers; `bin` entries point at raw `.ts` with
    a Bun shebang; no `files` / `publishConfig`; no registry scope
    reserved. This session exists to decide *what* we publish and
    *how*, before writing any pipeline.

- **ADR index reviewed.** ADRs relevant to this session's scope:
  - ADR-002 — `corpus/ → src/ → .literate/` invariant. The
    `.literate/` clause is a candidate for deprecation; the Effect
    rewrite + `node_modules/@literate/*` vendoring makes the
    snapshot redundant.
  - ADR-004 — CLI in Effect; Bun/Deno/Node compatible; manifest via
    `package.json.literate`. The compat matrix is a candidate for
    amendment (drop Node; keep Bun as required; Deno incidental).
  - ADR-007 — LF's own repo has no `.literate/`. If ADR-002's
    `.literate/` clause is deprecated, ADR-007 collapses into a
    tautology and is formally superseded.
  - ADR-009 — Tropes ship as workspace packages. Preserved; what
    changes is the *registry* (JSR vs npm) and the *publish format*
    (TS source vs compiled bundles).
  - ADR-015 — TypeScript composition + `.md` siblings via
    `prose(import.meta.url, …)`. Load-bearing for this session:
    compilation is lossy against this pattern; the publish format
    must preserve `.ts` + sibling `.md` colocation.
  - No new supersessions from prior sessions. ADR-019 (namespace)
    and ADR-020 (layout) remain in force.

- **Planned-session queue.** None. MVP arc fully closed. This is a
  fresh spontaneous session for v0.1 release engineering
  architecture.

- **Conversation-derived direction** (drafted in this session's
  opening turns before Goal gate):
  - The Effect rewrite + `@literate/*` npm/JSR packages already
    vendor Protocol prose into `node_modules/` on install;
    `.literate/` as a second vendored copy is redundant.
  - Bundling via `tsdown`/`tsup` loses the framework's meaning
    (sibling `.md` prose, Effect program readability, Trope
    narrative). Source-as-publish-artefact is the right invariant.
  - JSR (Deno-originated, TS-native registry) preserves TS source
    by design and is natively supported by Bun 1.1+ and Deno.
    Dropping Node support (not "Node compatible via loader" but
    "not supported") simplifies the matrix: Bun is required, Deno
    incidental, Node unsupported.
  - Consumer install flow: user installs Bun (via `mise`, curl,
    Homebrew), then `bunx jsr:@literate/cli …` or
    `bun add -g jsr:@literate/cli`. No npm. No compiled binary.
    No Node.

## Goals

### Goal 1 — Three ship-surface ADRs gated as a set

**Status:** Superseded by Goal 3
**Category:** feature
**Topic:** Author and gate three new ADRs that together define what
LF publishes, how consumers install, and what shape a consumer repo
takes. They are tightly linked — each constrains the others — so
the Goal gates the *set*, and each ADR is gated individually during
the work.

- **ADR-022 — Consumer `.literate/` role narrowed: init marker +
  `LITERATE.md` protocol entry + project config.** Amends ADR-002's
  `.literate/` clause (the "vendored LF snapshot" role is dropped —
  redundant post-Effect-rewrite with installed `@literate/*`
  packages carrying their own authored source per ADR-023). The
  folder itself stays in the consumer invariant, with a narrower,
  author-side role:
  - `.literate/` existing = this is an LF-governed repo (init
    marker for `literate init` and related CLI verbs).
  - `.literate/LITERATE.md` = authoritative, human- and
    agent-readable Protocol prose, scaffolded by `literate init`
    from an LF-maintained template; fully customisable by the
    consumer. Root `CLAUDE.md`, `AGENTS.md`, `.cursor/rules/*`,
    etc. become thin pointers to this file so any agent landing
    cold has one entry.
  - `.literate/config.*` = project-specific configuration
    (corpus path, agent defaults, wired Tropes). **Supersedes
    `package.json.literate`** from ADR-004, so non-JS consumers
    work natively without needing `package.json`.
  - `.literate/` does NOT vendor Concept/Trope prose; that lives
    in installed `@literate/*` packages.

  ADR-007 stays Accepted: LF's own repo still has no `.literate/`;
  root `CLAUDE.md` + `corpus/CLAUDE.md` fill the entry-file role.
  ADR-004's manifest clause is amended alongside ADR-002's
  `.literate/` clause.

- **ADR-023 — LF publishes source, not bundles.** Establishes the
  invariant that every `@literate/*` package ships authored
  TypeScript + sibling Concept/Trope prose, verbatim. No `tsdown`,
  no `dist/`, no minification. Downstream constraints: no
  single-file binary distribution (`bun build --compile` would lose
  self-similarity); tree-shaking / DCE is the consumer's build-time
  concern; the `prose(import.meta.url, …)` pattern (ADR-015) is
  preserved through install. Grounded in ADR-015's sibling-prose
  architecture: the framework's *meaning* is its authored source;
  compilation strips meaning.

- **ADR-024 — Publish on JSR; Bun is the required consumer
  runtime.** Amends ADR-004's runtime-compat clause. Drops Node
  compatibility from the consumer-runtime matrix. Bun is the
  required runtime; Deno is incidentally supported (JSR is
  Deno-native). Endorsed install path: `mise use -g bun@1.2` (or
  `curl -fsSL https://bun.sh/install | bash`) → `bunx
  jsr:@literate/cli` or `bun add -g jsr:@literate/cli`. JSR
  publishing requires GitHub OIDC, which we accept.

**Upstream:**

- ADR-002, ADR-004, ADR-007 (to be amended / superseded).
- ADR-009, ADR-015, ADR-019, ADR-020 (preserved; inform the shape).
- S4 `## Deferred / Discovered` — the publish/install story was
  explicitly deferred past MVP.
- This session's opening conversation — architectural reasoning
  for each ADR lives in the Pre-work above and in the authored ADR
  bodies.

**Scope:**

- Draft `corpus/decisions/ADR-022-consumer-literate-folder-narrowed.md`.
- Draft `corpus/decisions/ADR-023-publish-source-not-bundles.md`.
- Draft `corpus/decisions/ADR-024-jsr-bun-ship-surface.md`.
- Gate each (Accept / Correct / Clarify / Reject).
- On Accept of each: update `corpus/decisions/decisions.md`; set
  superseding `Status:` lines on ADR-002 (`.literate/` clause,
  manifest-adjacent), ADR-004 (runtime-matrix clause + manifest
  clause); append `## Decisions Made` bullets to this log.
- Mechanical file edits that follow from the accepted ADRs:
  - Replace `packages/template-minimal/files/.literate/.keep`
    placeholder with scaffolded `.literate/LITERATE.md` and
    `.literate/config.json` (ADR-022 consequence).
  - Update `packages/template-minimal/files/corpus/CLAUDE.md` to
    point at `.literate/LITERATE.md` rather than restate Protocol
    rules.
  - Update `packages/template-minimal/files/package.json` to drop
    the `literate` manifest key (moved to `.literate/config.json`).
  - Update `@literate/cli` to resolve config from
    `.literate/config.*` instead of `package.json.literate`
    (small edit in `runContinue` / `runClose` / the template's
    `scaffold()` helpers).
  - Root `CLAUDE.md` and `corpus/CLAUDE.md` of LF's own repo
    stay unchanged (ADR-007 preserved).

**Out of scope:**

- The publish *pipeline* itself (JSR scope registration, `jsr.json`
  authoring, GitHub Actions workflow, version-management script).
  That lands in a successor session planned out of this session's
  `## Plan` block.
- `literate init` verb (tracked in S4 Deferred; becomes easier once
  `.literate/` is gone but is still a distinct Goal).
- Curl installer script (`get.literate.sh`) and Homebrew tap.
  Belong to the successor.
- README updates for install documentation. Follow the successor.

**Acceptance:**

- Three new ADR files exist with `Status: Accepted`.
- `corpus/decisions/decisions.md` has three new rows, correct tags,
  and correct superseding `Status:` entries on ADR-002 (`.literate/`
  clause narrowed) and ADR-004 (runtime-matrix + manifest
  clauses). ADR-007 unchanged.
- `packages/template-minimal/files/.literate/LITERATE.md` and
  `.literate/config.json` exist with scaffolded content; `.keep`
  placeholder removed.
- `packages/template-minimal/files/corpus/CLAUDE.md` updated to
  point at `.literate/LITERATE.md`.
- `@literate/cli` resolves config from `.literate/config.*` (CLI +
  CLI tests updated accordingly).
- All prior-session docs referencing `.literate/` as a vendored
  snapshot are updated (or have a carry-over item logged).
- Typechecking still passes in every package (`bun run typecheck`).
- Test suite still green. The e2e smoke test's fixtures update to
  include `.literate/config.json`; expect pass count to stay
  30/30 (or shift slightly if test content changes).

**Notes:**

- ADR-002's amendment pattern follows the ADR-018 / ADR-019 /
  ADR-020 precedent: the ADR body stays append-only, only the
  `Status:` line changes to record the supersession of a specific
  clause.
- ADR-004 receives two clause-level amendments in this session
  (runtime matrix from ADR-024, manifest from ADR-022). Both
  recorded on its `Status:` line.
- ADR-007 is *preserved*, not superseded. LF's own repo continues
  to have no `.literate/` — the root `CLAUDE.md` + `corpus/CLAUDE.md`
  pair serves the entry-file role natively.
- The three ADRs are ordered so that ADR-023 and ADR-022 can be
  drafted and gated without ADR-024 (they're registry-agnostic).
  ADR-024's draft builds on the prior two.

### Goal 3 — Session P1: Publishing pivot (supersedes Goal 1 in part)

**Status:** Completed
**Category:** feature
**Topic:** The Person surfaced, mid-session, a distilled exploration
protocol at `temp/2026-04-24-exploration-protocol.md` that materially
reverses the ship-surface strategy Goal 1 encoded. Specifically:

- §1 flips the publishing registry from JSR to npm, flips the
  ship format from TypeScript source to bundled JavaScript, and
  collapses the consumer-runtime matrix to "any Node-shaped JS
  runtime" (Bun, Deno, and Node all run the CLI).
- §1.2 explicitly names ADR-023 and ADR-024 as superseded; ADR-004's
  runtime clause becomes trivially satisfied under the reframe.
- §1.3 addresses the reflexivity concern that motivated ADR-023 by
  relocating literate-programming discipline from the *distribution*
  level to the *principle* level (LF practices prose-first
  discipline in its own corpus and enforces it in consumer repos;
  the CLI as a compiled tool is not a violation of the principle
  but an implementation of it — "a hammer teaching carpentry
  doesn't need to be made of wood").
- §3 redefines `.literate/` as LF-generated (regenerated on every
  `literate compile`, direct edits undefined behaviour) rather than
  consumer-authored — which materially supersedes ADR-022's "init
  marker + LITERATE.md entry + config" framing. Consumer
  customizations move to `.literate/extensions/`; consumer Product
  prose stays in `corpus/`.
- §2.4 narrows ADR-005 ("Tropes are prose-first MDX"): prose-first
  *in authoring*, weave-function at *ship time*. The ship artefact
  changes from "TS + MDX sibling" to "TS only" (MDX inlined at
  build time).

Goal 1's three ADRs predate this exploration; two (ADR-022,
ADR-023) are Accepted and cannot be edited (IMP-6); one (ADR-024)
is still `Status: Open` and can be Rejected under the standard
ADR gate. The Protocol-correct path is supersession, not rewrite.

This Goal scopes the **Session P1 Publishing pivot** per the
exploration's §13.1 decomposition. P2–P8 (Trope ship shape,
proseSchema, override model, Disposition, Mode, Implication,
template finalisation) are successor sessions; they reshape Goal 2.

**Status:** Active
**Category:** feature
**Upstream:**

- `temp/2026-04-24-exploration-protocol.md` — the distilled
  exploration protocol authored by the Person (Discussion mode,
  Protocol disposition, per the exploration's own framing).
- ADR-022 (to be superseded — `.literate/` framing).
- ADR-023 (to be superseded — publish source).
- ADR-024 (to be Rejected — JSR + Bun).
- ADR-004 (preserved; its runtime clause becomes trivially true).
- ADR-005 (to be amended — prose-first in authoring, weave at ship).
- ADR-015 (preserved; the `proseFrom(import.meta.url, …)` pattern
  becomes a build-time transform target rather than a runtime
  filesystem read).

**Scope:**

- **Reject and delete** `corpus/decisions/ADR-024-jsr-bun-ship-surface.md`
  (standard Open → deleted transition; the ADR number returns to
  the pool and is immediately reused by the first new ADR below).
- **Author four new ADRs:**
  - `ADR-024` (new, supersedes ADR-022) — `.literate/` framing:
    LF-generated snapshot + `.literate/extensions/` customizations
    + `corpus/` for Product prose. Lifts §3 of the exploration
    verbatim at the level of invariants.
  - `ADR-025` (supersedes ADR-023) — LF publishes bundled
    JavaScript. Addresses the reflexivity argument at the
    principle level per §1.3.
  - `ADR-026` (new) — npm publishing with Trusted Publishing
    (OIDC via GitHub Actions). Replaces the JSR/OIDC shape from
    the rejected ADR-024.
  - `ADR-027` (new, amends ADR-005) — Tropes prose-first in
    authoring; weave-function at ship time; MDX inlined at
    build into a single TS file per Trope package. ADR-005's
    body stays append-only; its `Status:` records the amendment.
- **Atomic supersessions on Accept** of each new ADR:
  - ADR-022 `Status: Accepted (.literate/ clause narrowed by
    ADR-022)` → `Status: Superseded by ADR-024`.
  - ADR-023 `Status: Accepted` → `Status: Superseded by ADR-025`.
  - ADR-005 `Status: Accepted` → `Status: Accepted (amended by
    ADR-027)`.
  - ADR-002 `Status: Accepted (.literate/ clause narrowed by
    ADR-022)` → `Status: Accepted (.literate/ clause superseded
    by ADR-024; manifest clause TBD in a later session)`.
  - ADR-004 `Status: Accepted (manifest clause superseded by
    ADR-022)` → `Status: Accepted (manifest clause unresolved;
    runtime matrix unchanged under ADR-025/026)`. The manifest
    question is deferred — ADR-022 moved it to `.literate/config.*`
    but §3 places that under `.literate/extensions/` (which
    survives regeneration); the exact path lands in a later
    session.
- **Update `corpus/decisions/decisions.md`** with four new rows,
  superseded rows for ADR-022 and ADR-023, and amendment rows
  for ADR-002, ADR-004, ADR-005.
- **Append `## Decisions Made` bullets** to this session log for
  each ADR operation.

**Out of scope (deferred to P2–P8 per §13):**

- §2 Trope ship shape (build-time MDX inline transform) — **P2**.
- §6 `proseSchema` field on `Trope<C>` — **P3**.
- §4 Override and ejection model; §5 update reconciliation — **P4**.
- §8 Disposition Concept (renames Modality; supersedes ADR-021) —
  **P5**.
- §9 Mode Concept; §11 IMP-N Mode discipline imperative — **P6**.
- §10 Implication Concept — **P7**.
- §7 template minimal finalization + `.literate/extensions/`
  scaffold — **P8**.
- Any code changes at all. §13.1 says "No code changes yet; prose
  only." This session ends with the ADRs landed and the code in
  `packages/*` still pointing at the pre-pivot shape. P2 onward
  brings code into line.

**Acceptance:**

- `corpus/decisions/ADR-024-jsr-bun-ship-surface.md` does not exist
  on disk (Rejected/deleted).
- Four new ADR files exist at `ADR-024-<slug>.md`,
  `ADR-025-<slug>.md`, `ADR-026-<slug>.md`, `ADR-027-<slug>.md`,
  each with `Status: Accepted` (after gate).
- ADR-022, ADR-023, ADR-005 have updated `Status:` lines.
- ADR-002 and ADR-004 have refined `Status:` lines.
- `decisions.md` reflects the full graph.
- Session log's `## Decisions Made` has one bullet per new ADR +
  one bullet per supersession/amendment.
- No code in `packages/*` is touched.
- Typechecking status is unchanged from session open (30/30 tests
  still pass; no new tests; no code deltas).

**Notes:**

- This Goal supersedes **Goal 1** (three ship-surface ADRs per the
  pre-exploration direction). ADR-022 and ADR-023 stay on disk
  with their bodies intact (IMP-6); only their `Status:` lines
  change to record supersession. The Goal 1 body in this log
  also stays — its `Status:` flips to `Superseded by Goal 3` on
  Accept of this Goal, per IMP-3.
- **Goal 2 is also reshaped** by the exploration: it no longer
  plans *one* release-engineering successor; it plans the
  full P2–P8 arc. The Goal 2 entry in this log gets a
  parallel update — either as a Correct gate or a Goal 4
  supersession depending on how material the reshape is judged
  to be. Defer the decision until Goal 3 is Accepted and we
  reach session-close.

### Goal 2 — Plan the P2–P8 implementation arc

**Status:** Completed
**Category:** feature
**Topic:** Author a `## Plan` block at session close that scopes
the seven successor sessions named in exploration protocol §13
(P2 through P8), reshaped and pruned in light of the shadcn
pivot (ADR-025). The exploration's original P1 landed in Goal 3
of this session; P2–P8 carry the remaining implementation work
of the exploration's §2–§12. Under the shadcn pivot some of
§13's original entries collapse (e.g., the Trope ship shape /
MDX build-time inline transform of §13.2 dissolves because
Tropes no longer publish as packages) and one new entry emerges
(the registry fetch + `literate add`/`literate update` verbs).

The arc is pre-scoped via IMP-4 (optional multi-session-arc
planning) so that each successor session lands with a
`Status: Planned` log whose provisional Goals copy the exploration's
own decomposition, revisable at session open via IMP-1.6
re-gating.

**Upstream:**

- Goal 3 (Completed in this session) — ADR-024 + ADR-025 established
  the ship surface each successor session will implement against.
- `temp/2026-04-24-exploration-protocol.md` §2–§12 — the
  architectural material the successors implement; §13 — the
  original decomposition (adjusted for ADR-025).
- IMP-4 — multi-session arc planning procedure.

**Scope:**

Author Plan entries in this log for **six successor sessions**
(P4's merge-UX scope was Rejected as out of CLI responsibility
and its residual override-semantics question folded into P2):

- **P2 — Registry + CLI surface (tangle / weave / update) +
  override-semantics resolution.** Implement the CLI's
  operations from ADR-025:
  - `tangle` — fetch seed from registry, place in
    `.literate/tropes/<id>/...` or `.literate/concepts/<id>/...`,
    update `.literate/manifest.json`.
  - `weave` — read vendored tree → materialise
    `.literate/LITERATE.md`.
  - `update <kind> <id>` — re-fetch the seed at the current
    registry ref and **overwrite** the vendored file; consumer's
    own git workflow surfaces the diff and handles any merge. No
    three-way merge UX, no diff surfacer, no editor integration;
    the CLI is not a merge tool. Optional warning when the
    target file has git-unstaged local modifications.
  - Pick the registry fetch mechanism (open question §1 of
    ADR-025). Pick the version pinning granularity (open
    question §2). Resolve `.literate/extensions/overrides/` role
    (open question §3): most likely eliminated entirely since
    consumers edit vendored files directly; if repurposed, the
    target is consumer-authored *new* Tropes distinct from
    LF-seeded.
- **P3 — proseSchema as Trope contract.** Exploration §6 + §13.3.
  Add `proseSchema` field to `Trope<C>` in `@literate/core`;
  write `proseSchema` for each seeded Trope; add validation in
  the weave step. Applies to vendored Tropes — validation
  structurally (mdast-shape via remark + Effect Schema), not
  textually.
- **P5 — Disposition Concept.** Exploration §8 + §13.5.
  Author `@literate/concept-disposition` as a parametrized struct
  with Effect Schema (`base: Schema.Literal('Product') |
  Schema.Literal('Protocol') | Schema.Literal('Infrastructure')`,
  optional `scope: Schema.String`, optional `prompt:
  Schema.String`, optional `prose: Schema.String`). Revise
  session and ADR header shapes to carry Disposition. Update
  `trope-session-start` to set Disposition at session open.
- **P6 — Mode Concept + IMP-N (Mode-discipline imperative).**
  Exploration §9 + §11 + §13.6. Author `@literate/concept-mode`
  with Effect Schema over the Mode ADT (Exploring, Weaving,
  Tangling). **Name the enactor axis explicitly** (agent-in-session
  vs. mechanical CLI — flagged by ADR-025 §5). Revise
  `concept-session` to carry Mode. Revise `trope-session-start`
  to set Mode at open. Revise `trope-session-end` to validate
  Mode transitions. Author **IMP-N in `corpus/CLAUDE.md` and
  the consumer template** — the imperative that binds agent
  behaviour to the active Mode (Exploring resists tool-calling
  and drafting; Weaving drafts gated prose and does not derive
  code; Tangling derives code from accepted prose and does not
  author new prose; Mode is set at session-start, Mode shifts
  are gated). The imperative is load-bearing because agentic
  IDEs (Cursor, Zed, Claude Code) bias toward action by default;
  without it, an agent in Exploring Mode will collapse
  exploration into premature implementation.
- **P7 — Implication Concept (typed).** Exploration §10 + §13.7.
  Author `@literate/concept-implication` with **Effect Schema
  over a typed member struct** parallel to Mode and Disposition:
  `{ _tag: 'Implication', id: Schema.String, status:
  Schema.Union(Schema.Literal('Surfaced'),
  Schema.Literal('Promoted'), Schema.Literal('Filed'),
  Schema.Literal('Dismissed')), rationale: Schema.optional(String)
  — required when status = Dismissed }`. Revise `concept-session`
  to include `implications: Schema.Array(Implication)`. Revise
  `trope-session-end` to validate every Implication is in a
  terminal status at close (typed exhaustive match on the status
  union, not string comparison). Author
  `trope-implication-flow` (typed Steps, Effect-composed)
  analog to `trope-goal-flow`.
- **P8 — Template finalisation + registry trust.** Exploration
  §7 + §13.8 + open question §5 of ADR-025. Update
  `template-minimal` to the shadcn shape: seeds live in
  `registry/tropes/` and `registry/concepts/` in LF's own repo;
  template scaffolding is authored files + a `literate init`
  tangle sequence. Decide registry trust mechanics (checksums /
  signatures / TLS-only). Smoke-test `literate init minimal`
  end-to-end.

**Dependencies:**

- P2 is unblocked immediately after this session closes (Goal 3
  delivered the ship surface; P2 implements it).
- P3, P5, P6, P7 each depend on P2 (they exercise the
  tangle/weave surface). They are mostly independent of each
  other; P6's enactor-axis prose reads P5's Disposition cell
  treatment for coherence but does not block on it.
- P8 depends on P2 (needs the tangle surface to scaffold
  templates) and P6 (template carries Mode-aware `CLAUDE.md`
  and IMP-N). Comes last.

**Acceptance:**

- Six `Status: Planned` log files exist under
  `corpus/sessions/` with filenames following the
  `YYYY-MM-DDTHHMM-<slug>.md` convention (timestamps sequenced
  after this session's close to preserve temporal ordering in
  the index). Each log has Pre-work empty (populated at open),
  `## Goals` with provisional Goal drafts copied from the
  exploration's §12/§13 scope adjusted for ADR-025 (to be
  re-gated at open per IMP-1.6), `Planned by:` pointing at this
  session's log, `Depends on:` per the dependency graph above.
- This session's `## Plan` block has six entries, each
  annotated with the successor log's path under `Realised by:`
  (populated at the successor's open; at session close here the
  field reads `(pending)`).
- `corpus/sessions/sessions.md` has six new rows with
  `Status: Planned`.
- Goal 2 itself flips to `Status: Completed` once the Plan block
  is authored and the successors are stamped.

## Decisions Made

- **ADR-022 Accepted** — Consumer `.literate/` role narrowed: init
  marker + `LITERATE.md` protocol entry + `config.*` project
  configuration. Amends ADR-002's `.literate/` clause and ADR-004's
  manifest clause; preserves ADR-007. Includes a three-tier
  discovery path for Concept/Trope prose (LITERATE.md meta-prose,
  installed `@literate/*` packages as authoritative source, CLI
  Unweave-modality verbs as ergonomic surface).
  - Files: `corpus/decisions/ADR-022-consumer-literate-folder-narrowed.md`
    created; `corpus/decisions/ADR-002-corpus-src-literate-invariant.md`
    `Status:` updated; `corpus/decisions/ADR-004-cli-effect-manifest.md`
    `Status:` updated; `corpus/decisions/decisions.md` row added,
    ADR-002 + ADR-004 rows updated.

- **ADR-023 Accepted** — LF publishes source, not bundles. Every
  `@literate/*` package ships authored `src/` (TypeScript + sibling
  `.md`/`.mdx`) verbatim; no `tsdown`/`tsup`/`tsc --emit`, no
  `dist/`, no bundler. Corollaries: no single-file-binary
  distribution; no `.d.ts` emission (consumers infer from source);
  tree-shaking is the consumer's build-time concern. Preserves
  ADR-015's `prose(import.meta.url, …)` resolution end-to-end
  (authoring → publish → install → runtime read); makes ADR-022's
  Tier 2 discovery path authoritative.
  - Files: `corpus/decisions/ADR-023-publish-source-not-bundles.md`
    created; `corpus/decisions/decisions.md` row added.

- **ADR-024 Accepted** (new ADR-024 — reuses the number from the
  Rejected JSR+Bun draft). `.literate/` is an LF-generated snapshot,
  regenerated on every `literate compile` and fully overwritten;
  consumer Protocol-register customizations live in
  `.literate/extensions/` (survives regeneration); `corpus/` is
  the consumer's Product prose (LF never writes). Generated-file
  sigil makes the generated/authored distinction visible.
  `LITERATE.md` is the authoritative Protocol file at
  `.literate/LITERATE.md`, materialised from LF + consumer
  extensions; `CLAUDE.md` at repo root is a thin optional pointer
  (everything works without it); CLI also names
  `.literate/LITERATE.md` in help/error output. Config location
  deferred (interim: `.literate/extensions/config.json`).
  - **Supersedes ADR-022** (ADR-022 `Status:` → `Superseded by ADR-024`).
  - Files: `corpus/decisions/ADR-024-literate-as-generated-snapshot-plus-extensions.md`
    created; `corpus/decisions/ADR-022-consumer-literate-folder-narrowed.md`
    `Status:` updated; `corpus/decisions/ADR-002-corpus-src-literate-invariant.md`
    `Status:` refined; `corpus/decisions/ADR-004-cli-effect-manifest.md`
    `Status:` refined; `corpus/decisions/decisions.md` ADR-024 row
    added, ADR-022/ADR-002/ADR-004 rows updated.
  - Also: the prior-session ADR-024 draft (`ADR-024-jsr-bun-ship-surface.md`,
    `Status: Open`) Rejected and deleted on Accept of Goal 3; the
    number was returned to the pool and immediately reused by this
    ADR-024.

- **ADR-025 Accepted** — LF adopts a shadcn-shaped distribution
  model. Two classes shipped: `@literate/cli` as bundled JS on npm;
  seed files (authored TS + MDX for Tropes and Concepts) in LF's
  git repo under `registry/`, fetched on demand, vendored into
  the consumer's `.literate/tropes/<id>/` and
  `.literate/concepts/<id>/`. CLI has three mechanical operations:
  **tangle** (fetch + place seed), **weave** (materialise
  `.literate/LITERATE.md` from vendored tree), **explain/check**.
  **LF-the-framework performs no AI work**; weave and tangle are
  deterministic file operations. CLI does not import Tropes at
  runtime — Tropes are data (text) from its perspective;
  consumer's own TS toolchain runs Trope code. Two manifests:
  `literate.json` at repo root (consumer config, registries);
  `.literate/manifest.json` (CLI ledger of what's vendored).
  Consumer owns vendored files after tangle. No update
  guarantees — `literate update` is explicit, three-way-merge.
  Registry = git-repo-as-source, HTTP fetch of raw files; no
  registry service; no npm publishing for Tropes. Reflexivity
  honestly discharged (consumer receives literate source; CLI
  bundling acceptable because CLI is infrastructure, not
  pedagogy).
  - **Supersedes ADR-023** (ADR-023 `Status:` → `Superseded by
    ADR-025`).
  - **Amends ADR-019** (`@literate/*` npm scope narrowed to
    `@literate/cli` only; Trope/Concept ids are registry-path
    conventions).
  - **Amends ADR-024** (`.literate/tropes/` and
    `.literate/concepts/` ownership clauses: consumer-vendored
    after tangle, not CLI-regenerated; three-folder contract
    and LITERATE.md/weave-output regeneration preserved).
  - **Preserves ADR-005, ADR-015.**
  - Weave/tangle vocabulary + mechanical-not-AI invariant folded
    into this ADR (§2); no separate terminology ADR authored.
  - Five open questions flagged for subsequent sessions:
    registry fetch mechanism, version pinning granularity,
    override semantics collapse, update-diff UX, registry trust.
  - Files: `corpus/decisions/ADR-025-shadcn-shaped-distribution.md`
    created; `corpus/decisions/ADR-023-publish-source-not-bundles.md`
    `Status:` updated; `corpus/decisions/ADR-019-reinstate-literate-namespace.md`
    `Status:` updated; `corpus/decisions/ADR-024-literate-as-generated-snapshot-plus-extensions.md`
    `Status:` updated; `corpus/decisions/decisions.md` ADR-025
    row added, ADR-019/ADR-023/ADR-024 rows updated.
  - Also: two Open drafts discarded before this ADR — the
    earlier ADR-025 "publish bundled JavaScript" draft (Open,
    deleted when the weave-tangle terminology draft was
    sequenced) and the ADR-025 "weave-tangle terminology" draft
    (Open, deleted when the shadcn pivot arrived). Both slots
    reused; no residual ADR-025 conflict on disk.

- **Goal 3 scope consolidation.** The Goal originally planned
  four new ADRs (ADR-024, ADR-025, ADR-026, ADR-027 —
  `.literate/` framing, publish bundled JS, npm with Trusted
  Publishing, ADR-005 amendment). Under the shadcn pivot the
  substance collapses to **two ADRs**: ADR-024 (`.literate/`
  framing) and ADR-025 (shadcn distribution, which absorbs the
  publish-format, registry, CLI-verb-terminology, and
  mechanical-not-AI invariants). ADR-005 and ADR-015 do not
  require amendment (ship-shape question is moot because Tropes
  do not publish). npm with Trusted Publishing is the intended
  pipeline mechanic for `@literate/cli`; its mechanics land in
  the release-pipeline successor session rather than as a
  standalone ADR here. Goal 3's Topic (Session P1 Publishing
  pivot) is unchanged.

## Plan

Six Planned successor sessions, pre-scoped per IMP-4 from the
exploration protocol's §13 (adjusted for the shadcn pivot in
ADR-025). Each entry's provisional Goals land in the successor's
log as drafts; re-gated at the successor's open per IMP-1.6.

### P2 — Registry + CLI surface (tangle / weave / update) + override-semantics resolution

**Slug:** registry-and-cli-surface
**Depends on:** (this session's close)
**Realised by:** corpus/sessions/2026-04-25T0900-registry-and-cli-surface.md
**Goals:**

- **Topic:** Implement ADR-025's three mechanical CLI operations.
  `literate tangle <kind> <id>` fetches a seed from the registry
  and places it under `.literate/tropes/<id>/` or
  `.literate/concepts/<id>/`, updating `.literate/manifest.json`.
  `literate weave` reads the vendored tree and materialises
  `.literate/LITERATE.md` and any other Protocol-derived
  artefacts. `literate update <kind> <id>` re-fetches at the
  current registry ref and overwrites the vendored file; no
  merge UX (per Goal 2 Correct).
  **Upstream:** ADR-025; exploration §13.2 (adjusted).
  **Acceptance:** `literate tangle` + `literate weave` + `literate
  update` exist, are typechecked, are smoke-tested end-to-end in
  a scratch consumer repo. Registry fetch mechanism chosen
  (open question §1 of ADR-025). Version pinning granularity
  chosen (open question §2).
- **Topic:** Resolve the `.literate/extensions/overrides/` role
  under shadcn. Most likely collapse entirely (consumers edit
  vendored files directly); if retained, repurposed for
  consumer-authored new Tropes distinct from LF-seeded.
  **Upstream:** ADR-025 open question §3.
  **Acceptance:** Decision recorded (as a small ADR or inline in
  this session's `## Decisions Made`); `.literate/extensions/`
  subfolder shape settled.

### P3 — `proseSchema` as Trope contract

**Slug:** prose-schema-as-trope-contract
**Depends on:** P2 (Goal 2's second entry needs the tangle/weave
surface to exercise `proseSchema` validation).
**Realised by:** corpus/sessions/2026-04-25T1400-prose-schema.md
**Goals:**

- **Topic:** Add `proseSchema: Schema.Schema<ParsedMdx, any>`
  field to `Trope<C>` in `@literate/core`'s `kinds.ts`. Parse
  MDX with remark into mdast; validate structural shape per
  Trope. Failures become typed `ProseSchemaViolation { path,
  expected, got }` in the weave's Effect channel.
  **Upstream:** ADR-025 (mechanical weave); exploration §6 +
  §13.3.
  **Acceptance:** Every seeded Trope has a `proseSchema`. Weave
  step validates and surfaces typed violations. Smoke test
  covers a structurally-correct Trope and a structurally-broken
  one.

### P5 — Disposition Concept

**Slug:** disposition-concept
**Depends on:** P2 (needs the registry surface to seed
`@literate/concept-disposition`).
**Realised by:** corpus/sessions/2026-04-26T0900-disposition-concept.md
**Goals:**

- **Topic:** Author `@literate/concept-disposition` as a
  parametrised struct with Effect Schema:
  `{ base: 'Product' | 'Protocol' | 'Infrastructure',
     scope?: string, prompt?: string, prose?: string }`.
  Revise session and ADR header shapes to carry Disposition.
  Revise `trope-session-start` to set Disposition at open.
  Update the Protocol prose in `.literate/LITERATE.md` template
  to name the Concept.
  **Upstream:** Exploration §8 + §13.5; ADR-021 (Modality —
  retired into Disposition).
  **Acceptance:** `@literate/concept-disposition` exists in
  `registry/concepts/disposition/`. Session header shape updated.
  `trope-session-start` sets Disposition. ADR-021 `Status:`
  recorded as retired in favour of Disposition Concept.

### P6 — Mode Concept + IMP-N (Mode-discipline imperative)

**Slug:** mode-concept-and-imperative
**Depends on:** P2 (registry surface), P5 (Disposition is the
cross-axis; Mode × Disposition framing needs both).
**Realised by:** corpus/sessions/2026-04-26T1400-mode-concept-and-imperative.md
**Goals:**

- **Topic:** Author `@literate/concept-mode` with Effect Schema
  over the Mode ADT (`Exploring | Weaving | Tangling`). **Name
  the enactor axis explicitly**: a Mode is enacted either by an
  agent-in-session (gated, AI-assisted) or by the mechanical CLI
  (deterministic, non-AI). Same Mode Concept; different enactor;
  different per-enactor discipline.
  **Upstream:** ADR-025 §5 (enactor axis flagged); exploration
  §9 + §13.6.
  **Acceptance:** `@literate/concept-mode` exists with typed
  Schema. `concept-session` carries `mode: Mode`.
  `trope-session-start` sets Mode at open. `trope-session-end`
  validates Mode transitions.
- **Topic:** Author **IMP-N** in `corpus/CLAUDE.md` and in the
  consumer-template `CLAUDE.md`. The imperative binds agent
  behaviour to the active Mode: Exploring resists tool-calling
  and drafting; Weaving drafts gated prose and does not derive
  code; Tangling derives code from accepted prose and does not
  author new prose. Mode is set at session-start; Mode shifts
  are gated.
  **Upstream:** Exploration §11 + §13.6.
  **Acceptance:** IMP-N in both files; session-start Trope reads
  the Mode and enforces the imperative via the materialised
  `LITERATE.md` surface an agent reads before acting.

### P7 — Implication Concept (typed)

**Slug:** implication-concept
**Depends on:** P2 (registry surface).
**Realised by:** corpus/sessions/2026-04-27T0900-implication-concept.md
**Goals:**

- **Topic:** Author `@literate/concept-implication` with **Effect
  Schema** over a typed member struct: `{ _tag: 'Implication',
  id: string, status: 'Surfaced' | 'Promoted' | 'Filed' |
  'Dismissed', rationale?: string — required when status =
  Dismissed }`. Parallel shape to Mode and Disposition.
  **Upstream:** Exploration §10 + §13.7.
  **Acceptance:** `@literate/concept-implication` exists; Schema
  enforces the rationale-required-on-Dismissed invariant.
- **Topic:** Revise `concept-session` to include
  `implications: Schema.Array(Implication)`. Revise
  `trope-session-end` to validate every Implication is in a
  terminal status (`Promoted | Filed | Dismissed`) via typed
  exhaustive match on the status union — not string comparison.
  Author `trope-implication-flow` (typed Steps, Effect-composed)
  analog to `trope-goal-flow`.
  **Upstream:** Exploration §12.5 + §12.7.
  **Acceptance:** Session-end refuses to close a session with a
  non-terminal Implication; validation errors carry typed
  `ImplicationNotTerminal { id, currentStatus }` failures.

### P8 — Template finalisation + registry trust

**Slug:** template-finalisation-and-registry-trust
**Depends on:** P2 (tangle surface), P6 (Mode-aware template +
IMP-N).
**Realised by:** corpus/sessions/2026-04-27T1400-template-finalisation.md
**Goals:**

- **Topic:** Update `template-minimal` to the shadcn shape.
  Seeds live in `registry/tropes/` and `registry/concepts/` in
  LF's own repo. The `literate init minimal` flow tangles the
  repo skeleton (corpus/, .literate/extensions/, CLAUDE.md
  pointer, literate.json, package.json) and weaves the initial
  `.literate/LITERATE.md` from the seeded Tropes and Concepts.
  **Upstream:** Exploration §7 + §13.8; ADR-025 §1 + §2.
  **Acceptance:** Smoke test: `literate init minimal <target>`
  in a fresh tmp dir produces a ready-to-use LF-governed
  consumer repo; `literate continue .` in that repo reaches the
  gate without errors.
- **Topic:** Decide registry trust mechanics. v0.1 candidates:
  TLS-only (trust the git source's HTTPS); content-addressable
  (fetched seeds carry a checksum recorded in the registry's
  metadata); signed (GPG or sigstore). Default likely TLS-only
  at v0.1; leave room for upgrade.
  **Upstream:** ADR-025 open question §5.
  **Acceptance:** Decision recorded as a short ADR or inline in
  session `## Decisions Made`.

## Work Done

### Corpus

- **Created** `corpus/decisions/ADR-024-literate-as-generated-snapshot-plus-extensions.md`
  (Accepted; supersedes ADR-022; amends ADR-002 `.literate/` clause
  and ADR-004 manifest clause; preserves ADR-007).
- **Created** `corpus/decisions/ADR-025-shadcn-shaped-distribution.md`
  (Accepted; supersedes ADR-023; amends ADR-019 namespace clause
  and ADR-024 `.literate/tropes/` / `.literate/concepts/` ownership
  clauses; preserves ADR-005 + ADR-015; folds in the weave/tangle
  terminology and the "LF performs no AI work" invariant).

### Created then Rejected (deleted on disk; history preserved in
session log)

- `corpus/decisions/ADR-024-jsr-bun-ship-surface.md` — the
  pre-exploration JSR+Bun ship-surface ADR. Drafted `Status: Open`;
  Rejected and deleted on Accept of Goal 3. Number returned to
  pool; reused by the new `.literate/` framing ADR-024.
- `corpus/decisions/ADR-025-publish-bundled-javascript.md` — drafted
  `Status: Open`, deleted when the session shifted to gating the
  weave/tangle terminology amendment.
- `corpus/decisions/ADR-025-weave-tangle-terminology.md` — drafted
  `Status: Open`, deleted when the shadcn pivot arrived (the
  terminology is folded into the new ADR-025).

### Accepted-ADR `Status:` updates (body-frozen per IMP-6)

- `corpus/decisions/ADR-002-corpus-src-literate-invariant.md` —
  `Status:` updated to `Accepted (.literate/ clause superseded by
  ADR-024; src/ clause preserved)`.
- `corpus/decisions/ADR-004-cli-effect-manifest.md` — `Status:`
  updated to `Accepted (manifest clause unresolved post-ADR-024;
  runtime matrix trivially satisfied under ADR-025/ADR-026)`.
- `corpus/decisions/ADR-019-reinstate-literate-namespace.md` —
  `Status:` updated to `Accepted (npm-scope clause narrowed by
  ADR-025: @literate/* applies to @literate/cli only; Trope/Concept
  identifiers are registry-path conventions)`.
- `corpus/decisions/ADR-022-consumer-literate-folder-narrowed.md` —
  `Status:` updated to `Superseded by ADR-024`.
- `corpus/decisions/ADR-023-publish-source-not-bundles.md` —
  `Status:` updated to `Superseded by ADR-025`.
- `corpus/decisions/ADR-024-literate-as-generated-snapshot-plus-extensions.md`
  — `Status:` updated after Accept to record the shadcn-pivot
  amendment: `Accepted (.literate/tropes/ and .literate/concepts/
  ownership clauses amended by ADR-025; three-folder contract and
  LITERATE.md/weave-output regeneration preserved)`.

### Decisions-index updates

- `corpus/decisions/decisions.md` — rows added for ADR-024 and
  ADR-025; rows updated for ADR-002, ADR-004, ADR-019, ADR-022,
  ADR-023.

### Sessions

- **Modified** `corpus/sessions/sessions.md` — this session's row
  added (opened as `Status: Open`; closed as `Status: Closed
  (2026-04-24T17:00)`), six new `Status: Planned` rows for P2–P8.
- **Created** `corpus/sessions/2026-04-25T0900-registry-and-cli-surface.md`
  (P2; `Status: Planned`).
- **Created** `corpus/sessions/2026-04-25T1400-prose-schema.md`
  (P3; `Status: Planned`).
- **Created** `corpus/sessions/2026-04-26T0900-disposition-concept.md`
  (P5; `Status: Planned`).
- **Created** `corpus/sessions/2026-04-26T1400-mode-concept-and-imperative.md`
  (P6; `Status: Planned`).
- **Created** `corpus/sessions/2026-04-27T0900-implication-concept.md`
  (P7; `Status: Planned`).
- **Created** `corpus/sessions/2026-04-27T1400-template-finalisation.md`
  (P8; `Status: Planned`).

### Code

- None. Per Goal 3 acceptance criterion ("No code in `packages/*`
  is touched. Typechecking status is unchanged from session open;
  30/30 tests still pass") no code was modified. The ship-surface
  pivot is prose-only in this session; implementation lands in
  P2 onward.

## Deferred / Discovered

### Carry-forward Plan entries

All six P2–P8 Plan entries are carried forward as `Status: Planned`
successors (see `## Plan` block above and sessions.md rows). Each
successor's provisional Goals sit inside its log, ready for
re-gating at open per IMP-1.6. `Realised by:` fields will be
populated when each successor opens.

### Architecture material from the exploration not yet authored

The exploration protocol (`temp/2026-04-24-exploration-protocol.md`)
surfaced five open questions at the ADR-025 level, plus several
design questions the exploration itself flagged (§14). Ownership
of each:

- **ADR-025 open Q1** (registry fetch mechanism) — P2.
- **ADR-025 open Q2** (version pinning granularity) — P2.
- **ADR-025 open Q3** (override semantics collapse) — P2 Goal 2.
- **ADR-025 open Q4** (update-diff UX) — Rejected as CLI
  responsibility (Goal 2 Correct on P4); consumer's git handles
  diffs. No further work.
- **ADR-025 open Q5** (registry trust) — P8.
- **Exploration §14.1** (override metadata storage) — Rejected /
  moot alongside Q4.
- **Exploration §14.2** (one Mode Trope vs. one per Mode) — P6.
- **Exploration §14.3** (Subkind proseSchema symmetry) — P3.
- **Exploration §14.4** (Discussion as sub-stance of Exploring) —
  P6.
- **Exploration §14.5** (mid-session Mode transition gate
  mechanics) — P6.
- **Exploration §14.6** (Disposition on Trope vs. instance) —
  P5.
- **Exploration §14.7** (Disposition prose schema shape) — P5.
- **Exploration §14.8** (prompt-to-prose promotion) — P5.
- **Exploration §14.9** (agent scope-matching at session-start) —
  P5.

### Discovered in this session

- **The Mode Concept has an enactor axis.** A Mode is enacted
  either by an agent-in-session (gated, AI-assisted) or
  mechanically by the CLI (deterministic, non-AI). Same Mode
  Concept, different enactor, different per-enactor discipline.
  The CLI's `weave` and `tangle` operations (ADR-025 §2) are
  mechanical enactments of Mode Weaving and Mode Tangling under
  Disposition Protocol. This flag is picked up by P6 (Mode
  Concept) as an explicit Goal.
- **"LF-the-framework performs no AI work" is a load-bearing
  invariant, not just a design note.** ADR-025 §2 named it; the
  materialised `LITERATE.md` template should carry it prominently
  so agents reading the Protocol understand the mechanical/AI
  boundary without having to infer it. P8 owns this when the
  template finalises.
- **Three ADR-024 drafts were produced before the shadcn pivot
  reset the direction.** The volatility is recorded in Work Done;
  the net result is two ADRs (ADR-024 + ADR-025) ratified against
  the post-exploration direction. No archival loss — deleted
  drafts are cited in the session log; the Decisions Made bullets
  are self-describing.
- **`legacy/LITERATE.md` is now a frozen legacy artefact that
  shares a filename with the *materialised* `.literate/LITERATE.md`
  consumers will see.** ADR-024 §Consequences note the name-sharing
  but distinguishes the roles. Future-proofing note: if LF ever
  ships a versioned "canonical LITERATE.md template," the naming
  will want a version-aware distinguishing convention.
- **Goal 2 Correct narrowed P4 significantly.** The CLI-as-merge-
  tool framing the exploration §5 implied was Rejected; under
  shadcn the CLI re-fetches and overwrites, and the consumer's
  git workflow surfaces changes. P4 residual folded into P2.
  Worth keeping in mind for any later session tempted to
  re-introduce CLI-level reconciliation — that scope is
  consumer-owned.
- **The ADR-number reuse pattern was exercised twice** (ADR-024
  and ADR-025 each had rejected Open drafts whose numbers were
  reused). The adr-status.md `Open → (deleted; number returns
  to pool)` transition works as specified; no Protocol
  amendment needed.

## Summary

Landed the LF ship-surface pivot in two ADRs. **ADR-024** redefines
`.literate/` as an LF-generated snapshot (regenerated on every
weave; consumer customisation in `.literate/extensions/` survives
regeneration; `corpus/` is Product-register prose LF never writes);
`LITERATE.md` is authoritative at `.literate/LITERATE.md`;
`CLAUDE.md` is an optional root-level pointer. **ADR-025** adopts a
shadcn-shaped distribution model: `@literate/cli` is the sole npm
artefact (bundled JS); Tropes and Concepts live as authored TS +
MDX seeds in LF's git repo under `registry/`, fetched by the CLI
on demand and vendored into the consumer's `.literate/tropes/`
and `.literate/concepts/`; consumer owns vendored files after
tangle; no update merge UX (CLI is not a merge tool — consumer's
git handles diffs). The CLI's three operations are **tangle**
(fetch + place), **weave** (materialise the reader-facing
Protocol prose from the vendored tree), and **explain / check**;
all mechanical, deterministic, non-AI. The "LF-the-framework
performs no AI work" invariant is elevated to a named principle;
AI-driven authoring is the consumer's concern under the Protocol.
Six successor sessions (P2–P8) are pre-scoped per IMP-4: P2
implements the CLI surface; P3 adds `proseSchema` as Trope
contract; P5 authors the Disposition Concept (parametrised struct,
retiring Modality from ADR-021); P6 authors the Mode Concept with
the enactor axis and IMP-N (Mode-discipline imperative); P7 authors
the Implication Concept (typed); P8 finalises `template-minimal`
and picks registry trust mechanics. Three ADR-024 drafts and two
ADR-025 drafts were produced before the direction settled; the
volatility is recorded in Work Done, and the net ratified surface
is two ADRs with a clear amendment chain across the pre-existing
ADR graph.
