# Session: 2026-04-25 — `proseSchema` as Trope contract

**Date:** 2026-04-25 (provisional; rename on open per IMP-1.5 if drift)
**Status:** Planned
**Chapter:** — (no chapter yet)
**Agent:** (to be filled on open)
**Started:** (to be filled on open)
**Planned by:** corpus/sessions/2026-04-23T2100-ship-surface.md
**Depends on:** corpus/sessions/2026-04-25T0900-registry-and-cli-surface.md

## Goals

*Provisional — re-gate per IMP-1.6 at session open.*

### Goal 1 — Add `proseSchema` to `Trope<C>` and validate during weave

**Topic:** Extend `Trope<C>` in `@literate/core`'s `kinds.ts` with
`proseSchema: Schema.Schema<ParsedMdx, any>` where `ParsedMdx` is
the mdast-typed shape produced by remark parsing. Parse each
vendored Trope's `prose.mdx` with remark; validate structural
shape against the Trope's declared `proseSchema` during weave.
Failures become typed `ProseSchemaViolation { path, expected, got }`
in the weave's Effect error channel.

`proseSchema` is the **ship-side analogue** of `Concept<I>`'s
`instanceSchema`: Concepts constrain what consumers author as
instances; Tropes constrain what LF (or consumers via vendored
edits) author *as Tropes*. Same machinery, two scopes (§6 of the
exploration protocol).

**Upstream:** ADR-025 (mechanical weave); exploration §6 + §13.3.

**Acceptance:**
- Every seeded Trope in `registry/tropes/` carries a `proseSchema`.
- The weave step parses each Trope's prose, validates against
  `proseSchema`, and surfaces typed violations in the error
  channel.
- Smoke test covers one structurally-correct Trope and one
  structurally-broken Trope (e.g., missing a required heading).

**Notes:**
- `proseSchema` validates *structural markers that make a Trope a
  Trope*, not every paragraph. Example: "must contain `## Steps`
  heading with ≥5 ordered list items"; "if `index.ts` declares
  an Effect service, prose must reference it."
- The generated-file sigil from ADR-024 §3 is not subject to
  `proseSchema` validation. Weave strips the sigil before
  parsing the prose, or the Schema explicitly ignores it.

### Goal 2 — Subkind proseSchema symmetry (decide; flagged as exploration open question §14.3)

**Topic:** Does `Subkind` gain its own `proseSchema`? Symmetry
argument: yes, for consistency. Practical argument: ship later
if not needed at v0.1. Decide in this session based on the
seeded Trope set's actual use of Subkinds.

**Upstream:** Exploration §14.3.

**Acceptance:** Decision recorded; implemented if chosen,
deferred with rationale if not.
