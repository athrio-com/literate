# `step` — Concept seed

The typed shape of a Step *declaration* — id, kind (from
`step-kind`), description, optional version. Passive type
surface; the runtime `Step<I, O, E, R>` interface lives in
`@literate/core`. A Step declaration commits an author to the
contract before tangling code.
