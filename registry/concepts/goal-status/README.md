# `goal-status` — Concept seed

The closed vocabulary of `Status:` values a session-Goal can
carry: `Active | Completed | Abandoned | Superseded by Goal N`.
The `session-end` Trope's validator refuses to close a session
that carries a non-terminal Goal.
