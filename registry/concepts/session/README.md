# `session` — Concept seed

The typed shape of an LF session instance — its log path, slug,
`Status` lifecycle, agent identity, timestamps, and the three
optional cross-axis fields (`disposition`, `mode`,
`implications`). Distinct from the `session-start` and
`session-end` Tropes that operate over sessions; this Concept
declares the *shape* of one session.
