# `session-status` — Concept seed

The closed vocabulary of `Status:` values a session log can
carry: `Planned | Open | Closed | Abandoned`. The on-disk
suffix conventions (`Closed (timestamp)`,
`Abandoned (rationale)`) preserve the base value while
carrying machine-readable detail.
