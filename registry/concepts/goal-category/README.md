# `goal-category` — Concept seed

The closed vocabulary of `Category:` values a session-Goal can
carry: `exploration | feature | bugfix | refactor | prose |
process | migration | decision-only`. The `decision-only`
member names Goals whose Acceptance is a single decisive
artifact (an LFM authoring + gate cycle) without code change.
